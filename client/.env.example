# Origin of the Bingely API. The browser calls <this>/api/... directly.
# Default is http://localhost:4001. If you run the API on another port
# (e.g. PORT=4010 because your old server holds 4001), set it here.
VITE_API_BASE_URL=http://localhost:4001
